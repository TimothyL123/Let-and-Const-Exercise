const PI = 3.141592653589793;
if (true) {
    const PI = 42;
    console.log(PI);
}
console.log(PI);


// 1. The difference between "var" and "let" is that "var" can reassign, redeclare, and mutate. However, "let" cannot redeclare.

// 2. The difference between "var" and "const" is that "var" can reassign, redeclare, and mutate. However, "const" cannot reassign or redeclare.

// 3. The difference between "let" and "const" is that "let" can mutate and reassign but not redeclare, but "const" can only mutate and not reassign nor redeclare.

// 4. There variables are used before they are declared.